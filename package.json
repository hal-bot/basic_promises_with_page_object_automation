{
  "name": "protractor-typescript",
  "version": "0.0.1",
  "description": "Automated tests for the Haemonetics SafeTrace TX site",
  "scripts": {
    "default-test": "npm run pretest && protractor tmp/configs/defaultConfig.js",
    "saucelabs-test": "npm run pretest && protractor tmp/configs/sauceConfig.js",
    "pretest": "tsc",
    "start": "cd ../testapp && npm install && npm start",
    "test": "npm run default-test"
  },
  "author": "Hal Deranek (Slalom)",
  "license": "MIT",
  "dependencies": {
    "protractor": "^5.1.x",
    "typescript": "^2.x"
  },
  "devDependencies": {
    "@types/jasmine": "^2.5.x",
    "@types/node": "^6.0.62"
  }
}
