{
  "name": "haemonetics_safetrace_tx_automation",
  "version": "0.0.1",
  "description": "Automated tests for the Haemonetics SafeTrace TX site",
  "scripts": {
    "removeTmpDirectory": "rm -r tmp",
    "pretest": "tsc && npm run removeTmpDirectory && tsc",
    "default-tests": "npm run pretest && protractor-flake --max-attempts=2 -- tmp/configs/defaultConfig.js",
    "saucelabs-tests": "npm run pretest && protractor-flake --max-attempts=1 --  tmp/configs/sauceConfig.js",
    "test": "npm run default-tests"
  },
  "author": "Hal Deranek (Slalom)",
  "license": "MIT",
  "dependencies": {
    "protractor": "^5.1.x",
    "protractor-flake": "^2.5.0",
    "typescript": "^2.x"
  },
  "devDependencies": {
    "@types/jasmine": "^2.5.x",
    "@types/node": "^6.0.62"
  }
}
