{
  "name": "basic_promises_with_page_object_automation",
  "version": "1.0.0",
  "description": "This will give a basic example of how to work with promises inside a test automation project that uses the page object design pattern",
  "scripts": {
    "removeTmpDirectory": "rimraf tmp",
    "pretest": "tsc && npm run removeTmpDirectory && tsc",
    "test": "npm run pretest && protractor tmp/config.js"
  },
  "author": "Hal Deranek",
  "license": "MIT",
  "dependencies": {
    "protractor": "^5.1.2",
    "typescript": "^2.3.0"
  },
  "devDependencies": {
    "@types/jasmine": "^2.5.x",
    "@types/node": "^6.0.62"
  },
  "repository" : {
    "type" : "git",
    "url" : ""
  }
}
